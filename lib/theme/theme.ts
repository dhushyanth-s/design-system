import { createCss } from '@stitches/react'

export const {
  styled,
  css,
  global,
  keyframes,
  theme,
  getCssString,
  config,
} = createCss({
  theme: {
    colors: {
      secondaryColor: '#FEEAAD',
      primaryColor: '#DA9224',
      textColor: '#787878',
      grey: '#E2E2E2',
      backgroundColor: 'white',
    },
    shadows: {
      secondaryColor: '#FEEAAD',
      primaryColor: '#DA9224',
      textColor: '#787878',
      grey: '#E2E2E2',
      backgroundColor: 'white',
    },
  },
})
