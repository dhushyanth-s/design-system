import { createCss } from "@stitches/react";

export const {
  styled,
  css,
  global,
  keyframes,
  theme,
  getCssString,
  config,
} = createCss({
  theme: {
    colors: {
      backgroundColor: "#FEEAAD",
      primaryColor: "#DA9224",
      textColor: "#787878",
      grey: "#E2E2E2",
    },
  },
});
